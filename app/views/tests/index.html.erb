<h1>All Database Collection</h1>
<table>
    <thead>
        <tr>
         <th>Title</th>
         <th>Description</th>
         <th>Operations</th>
        </tr>
    </thead>

    <tbody>
        <% @tests.each do |test| %>
            <tr>
                <td><%= test.title %></td>
                <td><%= test.description %></td>
                <td><%= link_to 'Delete', test_path(test), method: :delete %></td>
                <td><%= link_to 'View', test_path(test) %></td>
                <td><%= link_to 'Edit', edit_test_path(test) %></td>
            </tr>
        <% end %>
    </tbody>
</table>
<%= link_to 'Create Nea Database', new_test_path %>